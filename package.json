{
  "name": "responsive-email",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mjml src/*.mjml -o build/",
    "watch": "mjml -w src/*.mjml -o build/",
    "serve": "cp -r src/images build/ && browser-sync start --server build --files \"build/*.html\"",
    "dev": "concurrently \"npm run watch\" \"npm run serve\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bootstrap": "^5.3.7",
    "mjml": "^4.15.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.0"
  }
}
